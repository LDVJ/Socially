version: '3'
services:
  api:
    build: .
    ports:
      # - <port on local host>:<port on container>
      - 8000:8000
    env_file:
      - ./.env
  # db:
  #   image: postgres
  #   environment:
  #     - POSTGRES_PASSWORD=password123
  #     - POSTGRES_DB=fastapi
    
  #   volumes:
  #     - postgres-db: /app/postgresql/data